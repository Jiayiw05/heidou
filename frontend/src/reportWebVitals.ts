const reportWebVitals = (onPerfEntry?: any) => {
  if (onPerfEntry && onPerfEntry instanceof Function) {
    // 暂时禁用 web-vitals 以避免兼容性问题
    console.log('Web vitals reporting is temporarily disabled');
  }
};

export default reportWebVitals;
